import { Link } from 'react-router-dom';
//import { useParams } from 'react-router-dom';

const PageOfDevice = () => {
    //const { id } = useParams();



    return (
        <div>
            <Link to={"/"}>Повернутися на головну</Link>
        </div>
    );
};

export default PageOfDevice;
