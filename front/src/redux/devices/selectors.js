export const selectDevices = (state) => state.devices.devices;
export const selectIsLoadingDevices = (state) => state.devices.loading;
export const selectDevicesError = (state) => state.devices.error;
