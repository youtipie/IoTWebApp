export const selectNetworks = (state) => state.networks.items;
export const selectIsLoadingNetworks = (state) => state.networks.isLoading;
export const selectNetworksError = (state) => state.networks.error;
